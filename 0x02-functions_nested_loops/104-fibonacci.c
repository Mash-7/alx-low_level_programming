#include <stdio.h>

/**
 * main - Find and print the first 98 Fibonacc numbers,
 * starting with 1 and 2.
 *
 * Return: Always 0.
 */
int main(void)
{
	int i, j, k;
	long int fn1, fn2, fib1, fib2, jk1, jk2;

	fn1 = 1;
	fn2 = 2;
	j = k = 1;
	printf("%ld, %ld", fn1, fn2);
	for
